namespace GemGrid.Audio
{
    /// <summary>Placeholder SFX identifiers for M1 hooks. Real clips are wired in later (M2/M8).</summary>
    public enum SfxId
    {
        BlockPlace,
        LineClear,
        GameOver,
        Restart
    }
}
