namespace GemGrid.Journey
{
    /// <summary>Minimal starter achievement set — "core cần thiết để framework hoạt động" per the implementation brief, not full content.</summary>
    public enum AchievementId
    {
        FirstLevelComplete,
        FirstThreeStarLevel,
        FirstChapterComplete,
        TenLevelsComplete
    }
}
